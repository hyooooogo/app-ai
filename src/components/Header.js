import React from 'react'

const Header = () => {
    return (
        <header style={{ height: '100%', backgroundColor: '#222528', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
          <h1 style={{ color: 'white', fontSize: '1.8em',  }}>記事投稿アプリケーション（AI）</h1>
        </header>
      );
}

export default Header